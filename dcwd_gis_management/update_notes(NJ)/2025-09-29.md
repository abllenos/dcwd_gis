# 2025-09-29 — LogPage search UX + loading bar

Changes
- Search bar polish on `src/components/LogPage.tsx`:
  - Enlarged Input.Search with clearer placeholder and aria-label.
  - Keyboard: Enter triggers search; Escape clears and cancels any active scan.
  - Inline controls: placed Search and Stop buttons beside the input.
- Visual feedback:
  - Added AntD `Progress` bar to visualize layer-wide scan progress.
  - Kept concise text: scanned pages • approx total • matches.
- Table integration:
  - While searching, the main table uses `layerSearchStore.results` and disables pagination.
  - Falls back to paginated `logStore.data` when search is inactive.

Notes
- No new axios instances; all HTTP uses the shared interceptor clients.
- No dependency changes.
- Verified with `npm run build`; only existing non-blocking warnings.