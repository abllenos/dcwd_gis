# 2025-09-30 — Progress bar fix + clear-on-layer-change

Changes
- Fixed inconsistent progress when search completes:
  - Separated totals into `totalRecords` and `totalPages` in `src/stores/layerSearchStore.ts`.
  - Track `lastFetchSize` and derive `totalPages = ceil(totalRecords / lastFetchSize)`.
  - Compute percent strictly as `scannedPages / totalPages` (pages vs pages), clamp at completion.
  - Estimate `totalPages` on end-of-data if the server doesn’t provide a count.
- Updated `LogPage` to use `totalPages` for progress and show `totalRecords` only as textual fallback.
- Clearing behavior on layer changes:
  - When user selects a new Map Layer, clear the search input and cancel any active scan.

Verification
- Ran `npm run build` — build succeeded with existing non-blocking warnings.
- No package.json changes; no new dependencies.

Files touched
- `src/stores/layerSearchStore.ts`
- `src/components/LogPage.tsx`